/**
 * LocalFileManager - Utility class for managing file storage in browser localStorage
 * Replaces AWS S3 functionality with local browser storage
 */

// LocalFileManager is now deprecated for file storage in localStorage due to privacy and security concerns.
// Please use backend API for file uploads and retrieval.

const LocalFileManager = {
  // All file storage methods are disabled.
};

export default LocalFileManager;
